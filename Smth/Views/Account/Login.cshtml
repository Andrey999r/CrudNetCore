@{
    ViewBag.Title = "Вход";
}

<h2 class="auth-title">Вход</h2>

<div class="auth-container">
    @if (ViewBag.Error != null)
    {
        <div class="error-message">@ViewBag.Error</div>
    }

    <form method="post" class="auth-form">
        <input type="text" name="username" placeholder="Логин или Email" class="auth-input" required />
        <input type="password" name="password" placeholder="Пароль" class="auth-input" required />
        <input type="submit" value="Войти" class="auth-submit" />
    </form>

    <p class="auth-footer">
        Не зарегистрированы? <a href="/Account/Register">Зарегистрироваться</a>
    </p>
</div>